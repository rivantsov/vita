{
  "ResetDatabase": "false",
  // Connection strings
  "MsSqlConnectionString": "Data Source=.;Initial Catalog=VitaBooks;Integrated Security=True;TrustServerCertificate=True",
  "MsSqlLogConnectionString": "Data Source=.;Initial Catalog=VitaBooksLogs;Integrated Security=True;TrustServerCertificate=True",
  // don't know why but on some machines it works only with IP address, not with localhost
  "ServiceUrl": "http://127.0.0.1:5600",
  "LogFilePath": "_vitaWebLog.log",
  // this key is used to sign/verify Jwt tokens; replace it with your own secret string, it can be any string
  "JwtSecret": "VitaBooksApiSecret_VitaBooksApiSecret_"
}    

